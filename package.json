{
  "name": "drone-intelligence-command-center",
  "version": "1.0.0",
  "description": "Enterprise Drone Inspection & AI Agent Platform",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:all": "npm run build --prefix backend && npm run build --prefix frontend",
    "test:all": "npm run test --prefix backend && npm run test --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
